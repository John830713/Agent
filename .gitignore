# mneme data directory (user-specific, not project data)
.mneme/

# mneme binary (tracked in repo for self-contained framework)
# mneme.exe

# OS files
Thumbs.db
.DS_Store

# Machine-specific resources (content not tracked by git)
/resources/
