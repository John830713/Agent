#!/bin/sh
# Pre-commit hook: INDEX chain integrity check
python "D:/Agent/resources/tools/common/chain-check/chain_check.py" --root "D:/Agent" --verify
if [ $? -ne 0 ]; then
    echo "ABORT: INDEX chain integrity check failed."
    exit 1
fi
echo "INDEX chain OK."
